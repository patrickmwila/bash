#!/bin/bash

# Open a terminal and type the below command:
echo set completion-ignore-case on | sudo tee -a /etc/inputrc

# Enter password. Restart terminal.

#If for some case you want to remove case insensitive, just edit /etc/inputrc file by removing theset 
# completion-ignore-case line.

#That's all.
